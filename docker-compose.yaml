version: "3.9"

services:
  main-be:
    container_name: main-be
    image: main-be:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8090:8090"
    restart: unless-stopped
